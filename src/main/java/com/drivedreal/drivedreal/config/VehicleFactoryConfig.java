package com.drivedreal.drivedreal.config;

// import org.springframework.beans.factory.annotation.Qualifier;
// import org.springframework.context.annotation.Bean;
// import org.springframework.context.annotation.Configuration;

// import com.drivedreal.drivedreal.domain.vehicle.factory.VehicleFactory;

// @Configuration
public class VehicleFactoryConfig {

    // @Bean
    // public VehicleFactory currentVehicleFactory(
    //         @Qualifier("gasolineVehicleFactory") VehicleFactory gasoline,
    //         @Qualifier("electricVehicleFactory") VehicleFactory electric,
    //         VehicleProperties properties) {

    //     return switch (properties.getEnergyType()) {
    //         case "electric" -> electric;
    //         case "gasoline" -> gasoline;
    //         default -> gasoline; // par défaut
    //     };
    // }
}